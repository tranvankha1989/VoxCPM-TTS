#!/usr/bin/env bash
# ===================================================
#  OmniVoice TTS Launcher for macOS (Double-Click)
# ===================================================

cd "$(dirname "$0")"
chmod +x ./start.sh 2>/dev/null || true
./start.sh
